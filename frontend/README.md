# Compliance Reviewer — Frontend

Next.js 14 (App Router) + TypeScript + Tailwind. See the [project README](../README.md) for the full setup.

```bash
cd frontend
npm install
echo "NEXT_PUBLIC_API_BASE_URL=http://localhost:8000" > .env.local
npm run dev
```
